/* 
    数据类型分为：原始类型（数字，字符串，布尔值，null，undefined），对象类型（对象）
    数字：是由64位浮点格式表示，用Math对象定义的可操作复杂的算术运算(Math.pow()求幂，Math.round()四舍五入，Math.sqrt()开平方...)
 */

// 

//--数字由于是二进制浮点数表示，小数之间的操作会有误差
// console.log(0.3 - 0.2, 0.2 - 0.1,0.3 - 0.2 == 0.2 - 0.1);

//--NaN ,全局变量，not a number，非数字值，可用全局函数isNaN()来判断是否为一个非数字值
// console.log(0/0);  
// console.log(Boolean(''));

//--Infinity ，全局变量，无穷大的判定
// console.log(1/0);  


